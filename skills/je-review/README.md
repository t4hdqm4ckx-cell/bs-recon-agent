# JE Review Skill
**Status:** Operational (v0.1.0)

Spec defined in `SKILL.md`. Validates proposed journal entries (format, account mapping, period, duplicates, plug-entry guard, sign convention) and routes them to the correct approver per `config/thresholds.yaml`.
